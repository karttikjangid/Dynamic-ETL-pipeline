[pytest]
markers =
    integration: marks tests that span multiple pipeline layers (deselect with '-m "not integration"')
filterwarnings =
    ignore:Field name "schema" in "GetSchemaResponse":UserWarning
